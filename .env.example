# BearifiedCo Portfolio Environment Variables

# =============================================================================
# HD LIVESTREAM - Direct OBS Streaming via Livepeer
# =============================================================================
# Enable direct HD streaming from OBS (bypasses pump.fun stream compression)
# Set to "true" to use Livepeer, "false" to use pump.fun stream only
NEXT_PUBLIC_DIRECT_STREAM_ENABLED=false

# Livepeer Studio API Key (get from https://livepeer.studio)
LIVEPEER_API_KEY=your_livepeer_api_key

# Stream ID from Livepeer (created when you set up a new stream)
LIVEPEER_STREAM_ID=your_livepeer_stream_id

# Playback ID from Livepeer (used for HLS playback URLs)
LIVEPEER_PLAYBACK_ID=your_livepeer_playback_id

# =============================================================================
# SETUP INSTRUCTIONS FOR HD STREAMING
# =============================================================================
# 1. Sign up at https://livepeer.studio
# 2. Create a new API key in the dashboard
# 3. Create a new Stream with the following settings:
#    - Name: "BEARCO 24/7 Stream"
#    - Profiles: 1080p (6000kbps), 720p (3000kbps), 360p (1000kbps)
# 4. Copy the Stream ID and Playback ID to this file
# 5. In OBS, set your stream settings:
#    - Server: rtmp://rtmp.livepeer.studio/live
#    - Stream Key: (from Livepeer dashboard)
#    - Output: 1920x1080 @ 6000 Kbps, x264/NVENC, Keyframe 2s
# 6. Set NEXT_PUBLIC_DIRECT_STREAM_ENABLED=true
# 7. Start streaming from OBS
#
# The system will automatically:
# - Use Livepeer HD stream when available
# - Fall back to pump.fun stream if Livepeer is offline
# - Keep displaying pump.fun metadata (viewer count, market cap)
#
# Estimated cost: ~$250/month for 24/7 streaming (80% cheaper than AWS)
